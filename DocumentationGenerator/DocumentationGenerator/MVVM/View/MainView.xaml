<Window x:Class="DocumentationGenerator.MVVM.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocumentationGenerator.MVVM.View"
        mc:Ignorable="d"
        Title="MainView" Height="800" Width="1000">
    <Grid>
        <Label Content="{Binding FileName}" Height="50" FontSize="12" HorizontalContentAlignment="Center" HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="0,10"/>
        <Label Content="{Binding Output}" HorizontalAlignment="Stretch" VerticalAlignment="Center" FontSize="12"/>
        <Button Content="Load File" Command="{Binding LoadFileCommand}" Width="100" Height="50" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10,10"/>
        <Button Content="Load Directory" Command="{Binding LoadDirectoryCommand}" Width="100" Height="50" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10,10"/>
        <Button Content="Save Docs" Command="{Binding SaveDocsCommand}" Width="100" Height="50" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10,10"/>
    </Grid>
</Window>
