<Window x:Class="DocumentationGenerator.MVVM.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" WindowStyle="None" AllowsTransparency="True"
        Title="MainView" Height="800" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border BorderBrush="WhiteSmoke" Background="WhiteSmoke" MouseDown="OnChromeBarMouseDown" Grid.Row="0">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Label Content="DG">
                        <Label.Resources>
                            <Style TargetType="Label">
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Height" Value="Auto"/>
                                <Setter Property="Width" Value="50"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                            </Style>
                        </Label.Resources>
                    </Label>
                    <Button Style="{DynamicResource ChromeBarToolButtons}" x:Name="FileButton" Content="File.." Click="OnFileButtonClick"/>
                    <Button Style="{DynamicResource ChromeBarToolButtons}" Content="Setings" Command="{Binding OpenSettingsMenuCommand}"/>
                    <Button Style="{DynamicResource ChromeBarToolButtons}" Content="Help"/>
                </StackPanel>
                <!-- Window buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{DynamicResource ChromeBarDefaultButtons}" Content="_" Click="OnMinimizeButtonClicked"/>
                    <Button Style="{DynamicResource ChromeBarDefaultButtons}" x:Name="MaximiseButton" Content="[  ]" Click="OnMaximizeButtonClicked"/>
                    <Button Style="{DynamicResource WindowCloseButton}" Content="X" Click="OnCloseButtonClicked"/>
                </StackPanel>
                
            </Grid>
        </Border>


        <!-- File Menu Popup -->
        <Popup x:Name="FilePopup" Style="{DynamicResource DefaultPopup}" PlacementTarget="{Binding ElementName=FileButton}"
               Placement="Bottom" AllowsTransparency="True" StaysOpen="False" PopupAnimation="Scroll">
            <Border>
                <StackPanel>
                    <Button Content="Load File/s" ToolTip="Load a file or multiple files selected from a directory." Command="{Binding LoadFileCommand}"/>
                    <Button Content="Load Directory" ToolTip="Load a directory of source files." Command="{Binding LoadDirectoryCommand}"/>
                    <Button Content="Save Doc.." ToolTip="Save Loaded Documentation to a PDF in given location." Command="{Binding SaveDocsCommand}"/>
                    <Button Content="Clear Docs" ToolTip="Will Clear All files that have been loaded." Command="{Binding ClearDocsCommand}"/>
                    <Separator/>
                    <Button Content="Exit"/>
                </StackPanel>
            </Border>
        </Popup>

        <Label Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Top"
               HorizontalContentAlignment="Left" VerticalContentAlignment="Center"
               FontWeight="Bold" FontSize="20" 
               Margin="10,10" Content="{Binding FileName}"/>

        <ScrollViewer Grid.Row="1"  Margin="10,70,10,10">
            <RichTextBox IsReadOnly="True" x:Name="myRichTextBox" VerticalScrollBarVisibility="Auto" Grid.Row="1" Margin="10,10"/>
        </ScrollViewer>
        
    </Grid>
</Window>
