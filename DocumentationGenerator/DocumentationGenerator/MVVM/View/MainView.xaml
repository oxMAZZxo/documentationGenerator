<Window x:Class="DocumentationGenerator.MVVM.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocumentationGenerator.MVVM.View"
        xmlns:helpers="clr-namespace:DocumentationGenerator.MVVM.Helpers"
        mc:Ignorable="d"
        Title="MainView" Height="800" Width="1000">

    <Window.Resources>
        <Style TargetType="Popup">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="LightGray"/>
                    <Setter Property="BorderBrush" Value="Gray"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="CornerRadius" Value="5"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Width" Value="140"/>
                    <Setter Property="Height" Value="170"/>
                    <Style.Resources>
                        <Style TargetType="StackPanel">
                            <Style.Resources>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="FontSize" Value="16"/>
                                    <Setter Property="Width" Value="Auto"/>
                                    <Setter Property="Height" Value="40"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Style.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="5"/>
                                        </Style>
                                    </Style.Resources>
                                </Style>
                            </Style.Resources>
                        </Style>
                    </Style.Resources>
                </Style>
            </Style.Resources>
        </Style>
        
        <Style TargetType="StackPanel">
            <Style.Resources>
                <Style TargetType="Button">
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="Width" Value="100"/>
                    <Setter Property="FontSize" Value="20"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="ColourPickerLabels" TargetType="Label">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10"/>
        </Style>

        <Style x:Key="ColourPickers" TargetType="xctk:ColorPicker">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="20,0"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <StackPanel Background="LightGray" Orientation="Horizontal">
            <Button x:Name="FileButton" Content="File.." Click="OnFileButtonClick"/>
            <Button Content="Setings" Command="{Binding OpenSettingsMenuCommand}"/>
            <Button Content="Help"/>
        </StackPanel>

        <!-- File Menu Popup -->
        <Popup x:Name="FilePopup" PlacementTarget="{Binding ElementName=FileButton}"
               Placement="Bottom" AllowsTransparency="True" StaysOpen="False" PopupAnimation="Scroll">
            <Border>
                <StackPanel>
                    <Button Content="Load File/s" ToolTip="Load a file or multiple files selected from a directory." Command="{Binding LoadFileCommand}"/>
                    <Button Content="Load Directory" ToolTip="Load a directory of source files." Command="{Binding LoadDirectoryCommand}"/>
                    <Button Content="Save Doc.." ToolTip="Save Loaded Documentation to a PDF in given location." Command="{Binding SaveDocsCommand}"/>
                    <Button Content="Clear Docs" ToolTip="Will Clear All files that have been loaded." Command="{Binding ClearDocsCommand}"/>
                    <Separator/>
                    <Button Content="Exit"/>
                </StackPanel>
            </Border>
        </Popup>

        <Label Grid.Row="1" HorizontalAlignment="Left" FontWeight="Bold" FontSize="20" Margin="10,10" Content="{Binding FileName}"/>
        <Button Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10,10" 
                Command="{Binding RefreshPreviewCommand}" Content="Refresh Preview"
                FontSize="16" FontWeight="Normal" Padding="10">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </Button.Resources>
        </Button>

        <ScrollViewer Grid.Row="1"  Margin="10,70,10,0">
            <RichTextBox IsReadOnly="True" x:Name="myRichTextBox" VerticalScrollBarVisibility="Auto" Grid.Row="1" Grid.ColumnSpan="4" Margin="10,10"/>
        </ScrollViewer>
        
    </Grid>
</Window>
