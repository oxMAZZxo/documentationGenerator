<Window x:Class="DocumentationGenerator.MVVM.View.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DocumentationGenerator.MVVM.View"
        xmlns:helper="clr-namespace:DocumentationGenerator.MVVM.Helpers"
        xmlns:viewModel="clr-namespace:DocumentationGenerator.MVVM.ViewModel"
        mc:Ignorable="d" WindowStyle="ToolWindow" ResizeMode="CanResize"
        Title="Settings" Height="450" Width="800">
    <Window.Resources>
        <helper:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Transparent" Background="WhiteSmoke" 
                CornerRadius="0,0,10,10">
            <StackPanel Margin="5,0" Orientation="Horizontal" Grid.Row="0">
                <RadioButton Style="{DynamicResource TabRadioButton}" Content="General" IsChecked="{Binding SelectedTab, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static viewModel:SettingsTabs.General}}"></RadioButton>
                <RadioButton Style="{DynamicResource TabRadioButton}" Content="Colours" IsChecked="{Binding SelectedTab, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static viewModel:SettingsTabs.Colours}}"></RadioButton>
                <RadioButton Style="{DynamicResource TabRadioButton}" Content="General" IsChecked="{Binding SelectedTab, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static viewModel:SettingsTabs.Fonts}}"></RadioButton>
            </StackPanel>
        </Border>

        <UserControl Background="WhiteSmoke" Grid.Row="1" Margin="10,10" 
                     Content="{Binding CurrentUserControl}">
            <UserControl.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </UserControl.Resources>
        </UserControl>

        <Button Style="{DynamicResource ModernButton}" Grid.Row="2" 
                Margin="5,5" FontSize="16" FontWeight="Light"
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Content="Close" Click="OnCloseButtonClicked"></Button>
    </Grid>
</Window>
